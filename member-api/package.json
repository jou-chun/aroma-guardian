{
  "name": "aroma-guardian-member-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply aroma-guardian-members --local",
    "db:migrate:remote": "wrangler d1 migrations apply aroma-guardian-members --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
